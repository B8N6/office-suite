name: Deploy to server

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Auto deploy note
        run: |
          echo "Server-side auto-deploy watcher is enabled."
          echo "Changes pushed to main are picked up on the server automatically."
